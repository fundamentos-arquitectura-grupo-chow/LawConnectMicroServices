import { CompletePaymentResource } from './complete-payment-resource';

describe('CompletePaymentResource', () => {
  it('should create an instance', () => {
    expect(new CompletePaymentResource()).toBeTruthy();
  });
});
